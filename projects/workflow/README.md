# Workflow skills

Skills para workflows de desarrollo y operación: SDD, GitFlow, seguridad,
documentación, testing, DDIA y caché.

El catálogo mantiene las variantes `common`, `claude-only` y `copilot-only` para
conservar la exposición por plataforma. La instalación se gobierna desde la raíz
de `skills-hub`.
